namespace Domain.Interfaces
{
    public interface ISoftDeletable
    {
        /*
        A FUTURO: Se podría reactivar el borrado lógico.
        bool IsDeleted { get; }
        DateTime? DeletedOnUtc { get; }

        void SoftDelete();
        void Restore();
        */
    }
}